<h1 align="center"><%= @category.name.capitalize + " Department" %></h1>
<div align="center">
<% if user_signed_in? && current_user.admin? %>
  <div class="row">
    <span class="label label-default"><%= link_to "Edit category name", edit_category_path(@category) %></span>
  </div>
<% end %>
  <%= will_paginate @category_listings %>
</div>
 <%= render 'card' %>
 <div align="center">
  <%= will_paginate @category_listings %>
</div>
