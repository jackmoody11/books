<h1 align="center">Listing categories</h1>
<br \>
<div class="container">
<%= render 'listing-categories', obj: @categories %>
</div>
    <% if current_user.admin? %>
    <br \>
      <div align="center">
        <%= link_to "New Category", new_category_path, class: "btn btn-primary"%>
      </div>
    <% end %>
    <div class="center">
      <%= will_paginate @categories %>
    </div>
